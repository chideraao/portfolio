import styled from "styled-components";

export const StyledProjects = styled.section`
  padding-top: 3.5rem;

  & .container {
    height: 100%;
  }

  & h2 {
    font-size: 2.2rem;
    font-weight: 600;
    color: var(--primary);
    padding-bottom: 0.9rem;
  }
`;
